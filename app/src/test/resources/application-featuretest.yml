
spring:
  cache:
    type: redis
    redis:
      cache-null-values: true
      host: localhost
      port: 6379

app:
  kakao:
    api:
      domain: domain
      api-key: api-key
  naver:
    api:
      domain: domain
      client-id: client-id
      client-secret: client-secret